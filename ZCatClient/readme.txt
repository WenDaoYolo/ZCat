名称：ZCat客户端程序
--------------------------------------------------------
运行环境：windows7及以上
--------------------------------------------------------
构建环境

编译环境：MinGW(GCC)或MSVC
字符编码：UTF-8

构建过程(build目录下)：
MinGW：cmake -G "Makefiles" ..  cmake --build .		
MSVC：cmake ..  cmake --build .
--------------------------------------------------------