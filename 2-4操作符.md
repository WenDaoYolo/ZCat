
单目操作符：操作数为1
```
~按位取反                              //操作的是二进制
int a=15;
	printf("%d\n",~a);                a的补码：0000.....1111(取决于及其字长)，取反后：1111...0000，原码：10000...10000=-16
```

```
!逻辑取反                              真(非0)变假(0)，假(0)变真(非0)
int a=0;
if(!a)
	printf("yes!\n");
```

```
后置++--                              先使用(参与运算)，后自增或自减
int a=3;
int* pa=&a;
printf("%d\n",*pa++);                pa先使用，然后*pa，最后pa++ 

前置++--                              先自增或自减，后使用(参与运算)
int arr[2] = {1,2};
int* pa = arr;
printf("%d\n", *++pa);               pa先++，然后使用p，然后*pa

++--涉及到变化和使用两个操作，前后置的规则针对的是两个操作，不要混淆，与运算符本身优先级无关
当++--单独使用时，其效果都一样
int a=5;
a++;
++a;

+(正,一般省略)，-(负)
int a=+5;
int b=-5;

sizeof()
求空间大小，单位字节，sizeof是操作符而非函数,返回类型无符号整型
1.求类型大小
printf("%d\n",sizeof(int));         //求int类型的大小
2.求数据大小：括号可以省略
int arr[5]={1,2,3,4,5};             
printf("%d\n",sizeof(arr));         //求数组arr的大小
double b=3.14;                      
printf("%d\n",sizeof b);            //求b的大小


所有表达式都有两个属性：值属性和类型属性
例如：
a=5,b=4,a+b;
则9为a+b的值属性，int为a+b的类型属性

sizeof()的原理：
不会执行()中的表达式
sizeof是在编译阶段执行的，不会执行表达式，编译器通过数据的类型属性去推算出结果，在运行阶段就会直接将括号内的表达式替换为结果

int a=4;
printf("%d\n",sizeof(a++));
printf("%d\n",a);                   //打印结果可以发现sizeof中的a++并没有执行

所以sizeof的操作数是表达式，但不会去执行表达式，例如a+b，或者函数调用(调用函数也是一种表达式)，但不能是函数名(函数名不是表达式)
注意：函数调用，sizeof分析的则是返回值的类型，所以调用的函数不能是void类型

```

指针相关操作符：
```
&:取地址操作符
*:解引用操作符
```

双目操作符：操作数为2

算术操作符
```
+，-，*，/，%
```
做整数除法时(操作数均为整数)，其结果也保留整数
```
int a=2;
printf("%d\n",a/3);
printf("%f\n",a/3.0);
```
取余操作符操作的对象必须都是整数
```
int a=10,b=3;
printf("%d\n",a%b);
```

```
(类型)强制类型转换
float a=3.14;
int b=(int)a;                       //强制将a转换为int类型然后赋值给b     (a只是强制类型转换没有赋值操作，所以不会改变a)
printf("%d\n",b);                   //注意，强制类型转换后得到的是临时的右值，不是可以修改的变量本身
                                    //在强转指针类型的时候，强转后的指针不可修改其指向，但可以修改其解引用的值
                                    
int a=5;
int b=6;

(char)a=3;       //不可修改的右值

int* pa=&a;
(char*)pa=&b;    //不可修改的右值(指针)

*(char*)pa=999;  //指针不可修改，但指针指向的值可以修改
```

移位操作符：
```
>>，<<                                          将对应的二进制位进行移位      
```
左移>>：二进制整体向左移动，高位溢出丢弃，低位补0
```
int a=15;        //补码：000...1111
a=a<<1;          //补码移位后：00...11110      原码：00...11110=30
```
---
右移<<：右移分为算术右移和逻辑右移，具体采用哪种右移取决于编译器(vs2022采用算术右移)

对于有符号数
```
算术右移：低位溢出丢弃，高位补原符号位
逻辑右移：低位溢出丢弃，高位固定补0
```

对于无符号数固定采用逻辑右移
```
int a=-1;
a=a>>1;
printf("%d\n",a);

算术右移:
原码：       1000...0001
补码：       1111...1111
补码右移1位： 1111...1111      低位1溢出丢弃，高位补符号位1
原码：       1000...0001      结果还是为-1
```
---

位操作符：
```
&按位与，|按位或，^按位异或     (注意这里指的都是按位与或和异或而不是逻辑与或和异或,操作的是对应的二进制位)
int a=15,b=14;
printf("%d\n",a&b);                             补码：000...1111&000...1110=000..1110  原码:正数原反补一致即000...1110=14
```

赋值操作符：
```
=，+=，-=，*=，\=，%=，>>=，<<=，&=,|=,^=

int a=10;
a +=33;                                        等价于a=a+33
```
连续赋值：必须先定义
```
//正确语法
int a,b,c;
a=b=c=10;
//错误语法
int d;
d=f=g=20;
```

关系操作符：
```
>,<,==,>=,<=                            用于数据之间值的比较,若用==来比较地址，字符等，则分别比较的是地址高低、ascll码值大小
```
逻辑操作符
```
逻辑与&&，逻辑或||
&&全1为1，否则为0
||全0为0，否则为1

逻辑截断：逻辑操作符的特性，在能确定逻辑值后不再继续执行后面的表达式
int a = 0, b = 2, c = 1, d = 2;
if (a && (b++))         //与运算a为0，表达式直接为0后面不再执行
	;
printf("%d\n", b);
if (c || d++)           //或运算c为1，表达式直接为1后面不再执行
	printf("%d\n", d);
```

三目操作符：操作数为3
三目操作符又称为条件操作符，三目运算符
条件？表达式1:表达式2            条件为真执行表达式1，条件为假执行表达式2(其本质就是分支语句)
```
int a=5>3?3:4;           5>3表达式为真，则执行表达式3，然后将3赋值给a

//(c>d?c:d)=5;           三目运算符返回的是值而非变量(c或d的值)，所以其返回值不能作为可修改的左值
```


其他操作符：

逗号表达式：由逗号隔开的一串表达式，表达式从左至右依次执行，逗号表达式的值取最右表达式的值
```
int a=0;
while(a++,a<=10);                             逗号表达式，从左到右依次执行，最后取表达式a<=10的结果然后参与while条件的判断
```

```
下标引用操作符：数组或指针的相关操作符，操作数2，下标和数组名(或指针变量名)	
int a[10]={0};
a[6]=0;
```

```
函数调用操作符	
scanf(...);
printf(...);
函数名(...);
括号内为参数,也是函数调用操作符的操作对象,所以函数操调用作符操作的操作数为1+n个(1为函数名也是操作对象，n是函数参数)
int add(int a,int b){                        //则操作数为2
	return a+b;
}
```

```
结构体成员操作符...
*,.,->
```

改变操作符的操作数顺序不影响表达式
```
例如：
a+b==b+a
a[4]==[a]4                                     //因为不影响，所以语法是正确的，了解即可，编码时还是选择常规的语法思维
...

int main() {
	int arr[5] = { 1,2,3,4,5 };
	printf("%d\n", 1[arr]);
	return 0;
}
```
---

表达式的求值：由操作符的优先级和结合性决定,同时过程中可能还会出现操作符的类型转换,也会影响求值

影响复合表达式求值的属性
1.操作符优先级
```
()聚组优先级最高	....   逗号表达式优先级最低	
大部分情况下优先级情况:单目操作符>双目操作符>三目操作符
```

2.操作符的结合律

3.操作符是否控制求值的顺序		
```
1)逻辑与，逻辑或的逻辑截断
2)三目操作符的判断分支求值
3)逗号表达式的固定末尾求值
```

先看操作符的优先级，优先级相同则取决于结合律	如:+是从左至右,!是从右至左,()聚组(固定结合律)
![[操作符优先级.png]]

```
即使掌握表达式求值的因素也不一定能很好的控制所有复合表达式求值
问题表达式，无效表达式，是不可控制的，要避免这些垃圾代码(歧义，无效，无意义等)
```

```
int b=1;
int a=(++b)+(++b)+(++b);         
不同编译器的处理方式不一样
vs编译器：所有++运行完再运算+            结果：12
gcc编译器：从左到右，++b，+，++b,+,++b   结果：9
```



