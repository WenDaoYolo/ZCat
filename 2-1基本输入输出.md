输出函数printf()
```
char a='x';
printf("hello\n");              //指定了输出格式
printf("%c",a);
```
输入函数scanf()
scanf对于多个参数的输入，若未规定输入格式，则默认用制表符、空格分隔，用回车结束输入
```
int a=0;
char b='0';
char c='2';
scanf("%d",&a);               
scanf("%c-%c",&b,&c);          //指定输入格式:字符-字符 -> 其本质是规定了scanf读取缓冲区的格式
```
格式说明符：
```
%d        有符号整型
%u        无符号整型
%f        单精度浮点型
%lf       双精度浮点型
%c        单字符型
%s        字符串型:传入字符指针,从指针处开始向后打印直到\0停止
%nd       整型以n个字节的宽度输出，默认右对齐
%m.nf     浮点型以m个字节的宽度输出，保留n位小数位，默认右对齐(m可省略,即.nf或.nlf)
%m.nlf
%-d       左对齐输出
%x        16进制
%o        8进制
%p        打印地址


例:
%-3u      以无符号整型占位3个字节左对齐输出
```

printf的参数实际为字符指针：从指针所指向的地址处开始向后打印内容(遇到\0停止打印)，用数据类型的格式来限制他的打印规则
```

printf("hello world");  ->传参实际传递的是字符串的首元素地址

const char* p="why?";   ->传递字符指针

printf(p);
```